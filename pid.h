// Constants
float THRESHOLD, KP, KD, ADJUST;

// Global variables
float pastError, currentError, leftIR, rightIR, leftEncoder, rightEncoder, leftSpeed, rightSpeed;

// double pastError, currentError, leftIR, rightIR, leftEncoder, rightEncoder, leftSpeed, rightSpeed;

void pid();